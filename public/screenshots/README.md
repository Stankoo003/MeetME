Drop project screenshot files here, named to match each project's `screenshot`
path in `src/data.ts` (e.g. `ora.png`, `pravilo-posta.png`). Recommended: PNG or
JPG, ~1200x750, under 300KB. Projects without a matching file fall back to the
placeholder pattern automatically.
